---
"@kitsunekode/kunai": patch
---

Improve discovery UX by preserving artwork on release-calendar entries, exposing discover/offline/download commands from browse, adding `--discover`, and making `/random` / `/surprise` use a cached randomized catalog pool instead of only reshuffling trending picks.

Also exposes discover/offline tuning in Settings, enriches release-calendar timing copy, makes offline-library rows read like local shelf cards, adds smoke-test recipes to Diagnostics, and smooths loading-shell status copy so healthy subtitle attachment or provider retry progress is not presented as an issue.
