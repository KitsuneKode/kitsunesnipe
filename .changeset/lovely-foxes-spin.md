---
"@kitsunekode/kunai": patch
---

Improve discovery UX by preserving artwork on release-calendar entries, exposing discover/offline/download commands from browse, and making `/random` / `/surprise` use a cached randomized catalog pool instead of only reshuffling trending picks.

Also smooths loading-shell status copy so healthy subtitle attachment is not presented as an issue.
